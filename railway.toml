[build]
dockerfilePath = "Conspectare.Api/Dockerfile"
watchPatterns = ["Conspectare.*/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
